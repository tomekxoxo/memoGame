{
  "name": "memorygame",
  "version": "1.0.0",
  "description": "memory game ",
  "main": "app.js",
  "scripts": {
    "compile": "node-sass -w ./style.scss ./style.css --source-map=true --output-style=expanded",
    "prefix": "postcss ./style.css --use=autoprefixer --map=false --output=./style.css --watch",
    "bundle": "browserify ./app.js -o ./bundle.js"
  },
  "browserslist": [
    "last 4 versions"
  ],
  "author": "Tomasz Kasprowicz",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "autoprefixer": "^9.8.5",
    "browserify": "^16.5.1",
    "node-sass": "^4.14.1",
    "postcss": "^7.0.36",
    "postcss-cli": "^7.1.1"
  }
}
